# ADR-001 — Frontend Stack

**Status:** Accepted

## Decision
React + TypeScript + Vite + Tailwind CSS + react-router-dom + React Context.

## Context
The product is a highly interactive browser UI and the frontend is already implemented with this stack.

## Consequences
Future backend integration should connect through services/API boundaries rather than replacing the frontend framework or state architecture.
